Babel==2.14.0 # (NOT DIRECTLY USED) integrated collection of utilities that assist in internationalizing and localizing (adapting to support various configurations) the web app
blinker==1.7.0 # (NOT DIRECTLY USED) provides a fast dispatching system that allows any number of interested parties to subscribe to events, or “signals”. signalling for Python objects
click==8.1.7 # (NOT DIRECTLY USED) used for creating command line interfacing when running the Flask application
colorama==0.4.6 # (NOT DIRECTLY USED) Makes ANSI escape character sequences (for producing colored terminal text and cursor positioning) work under MS Windows.
Flask==2.3.3 # the web application framework, used to create routing and handle backend operations
Flask-BabelEx==0.9.4 # (NOT DIRECTLY USED)  adds internationalization and localization support to any Flask application with the help of babel, pytz and speaklater. It has builtin support for date formatting with timezone support as well as a very simple and friendly interface to gettext translations.
Flask-Login==0.6.3 # provides user session management for Flask. It handles the common tasks of logging in, logging out, and remembering your users’ sessions over extended periods of time.
Flask-Mail==0.9.1 # (NOT DIRECTLY USED) provides a simple interface to set up SMTP with the Flask application and to send messages from view functions and scripts
Flask-Principal==0.4.0 # (NOT DIRECTLY USED) a framework that allows us to flexibly integrate authentication and user information providers into the Flask app
Flask-Security==3.0.0 # allows to quickly add common security mechanisms to the Flask application
Flask-SQLAlchemy==3.1.1 # integration of Flask with SQLAlchemy
Flask-Admin==1.6.1 # simple admin interface framework for Flask
Flask-WTF==1.2.1 # integration of Flask with WTForms
greenlet==3.0.1 # (NOT DIRECTLY USED) lightweight coroutines for in-process sequential concurrent programming
install==1.3.5 # A package to install pip packages from within code
itsdangerous==2.1.2 # (NOT DIRECTLY USED) Various helpers to pass data to untrusted environments and to get it back safely. Data is cryptographically signed to ensure that a token has not been tampered with.
Jinja2==3.1.2 # allows Python to HTML interactivity by allowing special placeholders in HTML templates to pass data in Python code and use pythonic syntax within the template
MarkupSafe==2.1.3 # (NOT DIRECTLY USED) implements a text object that escapes special characters so it is safe to use in HTML and XML
passlib==1.7.4 # password hashing library which provides cross-platform implementations of over 30 password hashing algorithms and a framework for managing existing password hashes
pytz==2023.3.post1 # (NOT DIRECTLY USED) allows accurate and cross platform timezone calculations
speaklater==1.3 # (NOT DIRECTLY USED) provides lazy strings for translations within Flask package
SQLAlchemy==2.0.23 # (NOT DIRECTLY USED) Python SQL toolkit and Object Relational Mapper, used to create classes that represent tables of a relational database and to create objects (rows) and query using pythonic syntax
typing_extensions==4.8.0 # (NOT USED DIRECTLY) enables new type system features to be used on older Python versions, and enables experimentation with type system features proposed in new Python enhancement proposals before added to the typing module
Werkzeug==3.0.1 # used to create a web server gateway interface compatible web app, and to generate and check password hashes against plaintext password
WTForms==3.1.1 # (NOT DIRECTLY USED) used to implement various types of Form classes and instantiate Form objects to implement forms in rendered html and easily pull form data in Python
